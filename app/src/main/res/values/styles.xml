<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="windowActionBar">true</item>
        <item name="windowNoTitle">true</item>
        <item name="colorPrimary">@color/white</item>
        <item name="colorPrimaryDark">@color/royalBlue</item>
        <item name="colorAccent">@color/nordicBlue</item>
    </style>

    <!-- Custom theme for camera floating action button because FAB uses colorAccent as background color -->
    <style name="fab_style" parent="Widget.Design.FloatingActionButton">
        <item name="colorPrimary">@color/white</item>
        <item name="colorPrimaryDark">@color/royalBlue</item>
        <item name="colorAccent">@color/royalBlue</item>
    </style>

    <style name="AppTheme.FloatingActionButtonOverlay">
        <item name="colorAccent">@color/lightGray</item>
    </style>

</resources>
